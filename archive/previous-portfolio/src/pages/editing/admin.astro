---
import Layout from '../../layouts/Layout.astro';
import EditingNavigation from '../../components/EditingNavigation.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import LanguageToggle from '../../components/LanguageToggle.astro';
---

<Layout title="Admin Panel - Andrew's Video Editing">
  <ThemeToggle />
  <LanguageToggle />
  <EditingNavigation currentPath="/editing/admin" />
  
  <main class="min-h-screen">
    <!-- Login Form (shown when not logged in) -->
    <div id="login-section" class="container mx-auto px-4 py-16 max-w-md">
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8">
        <h1 class="text-3xl font-bold text-gray-800 dark:text-white mb-6 text-center" data-i18n="admin.login.title">Admin Login</h1>
        <form id="login-form" class="space-y-4">
          <div>
            <label for="password" class="block text-gray-700 dark:text-gray-300 mb-2" data-i18n="admin.login.password">Password</label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-600 dark:bg-gray-700 dark:text-white"
              required
            />
          </div>
          <div id="error-message" class="hidden text-red-600 dark:text-red-400 text-sm" data-i18n="admin.login.error">
            Invalid password
          </div>
          <button 
            type="submit"
            class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300"
            data-i18n="admin.login.button"
          >
            Login
          </button>
        </form>
      </div>
    </div>

    <!-- Admin Panel (shown when logged in) -->
    <div id="admin-panel" class="hidden container mx-auto px-4 py-16 max-w-6xl">
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-4xl font-bold text-gray-800 dark:text-white" data-i18n="admin.title">Admin Panel</h1>
        <button 
          id="logout-btn"
          class="bg-red-600 hover:bg-red-700 text-white font-semibold px-6 py-2 rounded-lg transition-colors duration-300"
          data-i18n="admin.logout"
        >
          Logout
        </button>
      </div>

      <!-- Portfolio Management -->
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8 mb-8">
        <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-4" data-i18n="admin.portfolio.title">Manage Video Portfolio</h2>
        <p class="text-gray-600 dark:text-gray-300 mb-4">
          Manage your video editing portfolio. Add, edit, and delete video entries through the CMS.
        </p>
        <div class="flex gap-4 flex-wrap">
          <a 
            href="/admin"
            target="_blank"
            class="bg-purple-600 hover:bg-purple-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300 inline-flex items-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z" />
            </svg>
            Open Video CMS
          </a>
          <a 
            href="/editing/portfolio"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300 inline-flex items-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            View Portfolio
          </a>
        </div>
        <div class="mt-4 p-4 bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg">
          <p class="text-sm text-blue-800 dark:text-blue-200">
            <strong>Tip:</strong> Use the CMS to manage video content. Changes are saved to <code>src/content/videos/</code> and automatically deployed.
          </p>
        </div>
      </div>

      <!-- Projects Management -->
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8 mb-8">
        <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-4">Manage Software Projects</h2>
        <p class="text-gray-600 dark:text-gray-300 mb-4">
          Manage your software engineering projects. Add, edit, and showcase your technical work.
        </p>
        <div class="flex gap-4 flex-wrap">
          <a 
            href="/admin"
            target="_blank"
            class="bg-indigo-600 hover:bg-indigo-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300 inline-flex items-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
            </svg>
            Open Projects CMS
          </a>
          <a 
            href="/projects"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300 inline-flex items-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
            View Projects
          </a>
        </div>
        <div class="mt-4 p-4 bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg">
          <p class="text-sm text-blue-800 dark:text-blue-200">
            <strong>Tip:</strong> Use the CMS to add projects with details like technologies, GitHub links, and live demos. Projects are saved to <code>src/content/projects/</code>.
          </p>
        </div>
      </div>

      <!-- Experience Management -->
      <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-8">
        <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-4" data-i18n="admin.experience.title">Manage Experience</h2>
        <p class="text-gray-600 dark:text-gray-300 mb-4">
          Manage your experience section including client success stories and testimonials.
        </p>
        <div class="flex gap-4 flex-wrap">
          <a 
            href="/editing/experience"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-300 inline-flex items-center gap-2"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            View Experience Page
          </a>
        </div>
        <div class="mt-4 p-4 bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-lg">
          <p class="text-sm text-yellow-800 dark:text-yellow-200">
            <strong>Note:</strong> To edit experience items, modify the data in <code>src/pages/editing/experience.astro</code>
          </p>
        </div>
      </div>
    </div>
  </main>

  <footer class="bg-gray-800 dark:bg-gray-950 text-white py-8 mt-16">
    <div class="container mx-auto px-4 text-center">
      <p class="mb-2">&copy; {new Date().getFullYear()} Andrew. <span data-i18n="footer.rights">All rights reserved.</span></p>
      <p class="text-gray-400 dark:text-gray-500" data-i18n="footer.services">Professional Video Editing & Subtitle Services</p>
    </div>
  </footer>
</Layout>

<script>
  import { getDefaultLanguage, t, type Language } from '../../i18n/translations';
  
  // ⚠️ SECURITY WARNING: This is a client-side only authentication demo.
  // The password is visible in the client-side JavaScript bundle.
  // For production use, implement proper server-side authentication.
  // This is suitable for personal use only, not for protecting sensitive data.
  
  // Simple password authentication (for demonstration only)
  // In production, use proper authentication with backend
  const ADMIN_PASSWORD = import.meta.env.PUBLIC_ADMIN_PASSWORD || 'admin123';
  
  const loginSection = document.getElementById('login-section');
  const adminPanel = document.getElementById('admin-panel');
  const loginForm = document.getElementById('login-form') as HTMLFormElement;
  const passwordInput = document.getElementById('password') as HTMLInputElement;
  const errorMessage = document.getElementById('error-message');
  const logoutBtn = document.getElementById('logout-btn');

  // Check if already logged in
  const isLoggedIn = sessionStorage.getItem('adminLoggedIn') === 'true';
  
  if (isLoggedIn) {
    loginSection?.classList.add('hidden');
    adminPanel?.classList.remove('hidden');
  }

  // Handle login
  loginForm?.addEventListener('submit', (e) => {
    e.preventDefault();
    
    if (passwordInput.value === ADMIN_PASSWORD) {
      sessionStorage.setItem('adminLoggedIn', 'true');
      loginSection?.classList.add('hidden');
      adminPanel?.classList.remove('hidden');
      errorMessage?.classList.add('hidden');
    } else {
      errorMessage?.classList.remove('hidden');
    }
  });

  // Handle logout
  logoutBtn?.addEventListener('click', () => {
    sessionStorage.removeItem('adminLoggedIn');
    adminPanel?.classList.add('hidden');
    loginSection?.classList.remove('hidden');
    passwordInput.value = '';
    errorMessage?.classList.add('hidden');
  });

  // Update page content based on language
  function updatePageContent() {
    const lang: Language = (localStorage.getItem('language') as Language) || getDefaultLanguage();
    
    document.querySelectorAll('[data-i18n]').forEach((element) => {
      const key = element.getAttribute('data-i18n');
      if (key) {
        element.textContent = t(key as any, lang);
      }
    });
  }

  // Update on page load
  updatePageContent();

  // Listen for language changes
  window.addEventListener('languagechange', updatePageContent);
</script>
